# Backend origin. Leave VITE_API_URL empty in dev — Vite proxies /api to VITE_API_PROXY.
# In production set VITE_API_URL to the backend origin (e.g. https://api.example.com).
VITE_API_URL=""
VITE_API_PROXY="http://localhost:5000"
